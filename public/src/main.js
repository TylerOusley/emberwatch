import * as THREE from 'three';
import { createWorld } from './world.js';
import { createCharacter } from './characters.js';
import { icon } from './icons.js';
import { PoseBuffer } from './motion.js';
import { createVillageChat } from './chat.js';
import { chooseInteraction, nearestGatherable } from './interactions.js';
import { CONFIG, BUILDINGS, ROAD, RESOURCES, TOOLS, WALLS, PLOTS, WORLD_BOUNDS, plotFront, plotSolids, moveWithCollision } from '/shared/world.js';
import { TOOL_TIERS, carryCapacity, inventoryWeight } from '/shared/content.js';
import { TRANSPORT } from '/shared/transport.js';
import { createSettlementUI } from './settlement-ui.js';
import { createTransportWorld } from './transport-world.js';
import { canEquip, FOOD_IDS } from '/shared/equipment.js';
import { bindCameraLook } from './camera-controls.js';

const $=id=>document.getElementById(id);
const escapeHTML=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pretty=n=>Math.max(0,Math.floor(Number(n)||0)).toLocaleString();
let state=null, me=null, ownId=null, selected='', role='villager', mode='register', auth=null, villageId=null, villages=[], requestedVillageName=null;
let socket=null, joined=false, connecting=false, intentionalClose=false, reconnectTimer=null, lastStateAt=0, cameraYaw=0, cameraPitch=.39, cameraDistance=8.5, tour=false, muted=false;
let cameraLook=null;
let localActionId=0, lastToolUse=-Infinity, interaction=null, audioContext=null, activePanel=null, dynamicSolids=[];
let hotbar=['sword','axe','pickaxe','scythe','hammer','food','bow','good_food'],savedHotbar=false;
try{const saved=JSON.parse(localStorage.getItem('emberwatch-hotbar')||'null');if(Array.isArray(saved)&&saved.length===8&&saved.every(id=>['sword','axe','pickaxe','scythe','hammer','bow','food','good_food','best_food','heal'].includes(id))){hotbar=saved;savedHotbar=true;}}catch{}
const poseTracks=new Map();
const keys=new Set(), actors=new Map(), desired={x:0,z:0,yaw:Math.PI}, predicted={x:0,z:4};
const dialog=$('panel-dialog');
const chat=createVillageChat({host:$('village-chat'),send,ownId:()=>ownId,onFocusChange:()=>{keys.clear();desired.x=desired.z=0;cameraLook?.stop();sendInput();}});
const settlement=createSettlementUI({getState:()=>state,getMe:()=>me,getActivePanel:()=>activePanel,openPanel,send,toast,getHotbar:()=>hotbar,setHotbar:(slot,id)=>{hotbar[slot]=id;savedHotbar=true;try{localStorage.setItem('emberwatch-hotbar',JSON.stringify(hotbar));}catch{}renderHotbar();}});
try{auth=JSON.parse(sessionStorage.getItem('emberwatch-session')||'null');}catch{}
const roles={guard:{name:'Guard',description:'40 shield · regenerates after six seconds without damage',icon:'guard'},priest:{name:'Priest',description:'125 health · heal and revive allies',icon:'priest'},villager:{name:'Villager',description:'150 base carrying capacity · 50 extra with every backpack',icon:'villager'}};
const toolDescriptions={sword:'Hold your ground',axe:'Gather timber',pickaxe:'Gather stone',scythe:'Harvest wheat',hammer:'Restore the gate',food:'Restore your hunger',heal:'Heal or revive an ally'};
for(const [id,r] of Object.entries(roles)){
  const b=document.createElement('button');b.type='button';b.innerHTML=icon(r.icon)+r.name;b.dataset.role=id;b.role='radio';b.title=r.description;b.setAttribute('aria-checked',id===role);b.classList.toggle('active',id===role);
  b.onclick=()=>{role=id;document.querySelectorAll('[data-role]').forEach(e=>{e.classList.toggle('active',e.dataset.role===id);e.setAttribute('aria-checked',e.dataset.role===id);});};$('role-options').append(b);
}
const foodIds=FOOD_IDS;
const itemName=id=>!id?'Empty hands':foodIds.includes(id)?({food:'Bread',good_food:'Hearty meal',best_food:'Feast'}[id]):id==='heal'?'Priest blessing':`${TOOL_TIERS[me?.tiers?.[id]||'wood']?.name||'Wooden'} ${id}`;
function renderHotbar(){
 $('hotbar').replaceChildren();hotbar.forEach((id,index)=>{const b=document.createElement('button');b.className='slot';b.dataset.tool=id;b.title=itemName(id)+' ('+(index+1)+')';b.setAttribute('aria-label',b.title);b.innerHTML=`<span class="slot-key">${index+1}</span>${icon(foodIds.includes(id)?'food':id)}${['sword','axe','pickaxe','scythe','hammer','bow'].includes(id)?'<span class="durability"><i></i></span><span class="slot-tier"></span>':'<span class="slot-count"></span>'}`;b.onclick=()=>selectTool(id);$('hotbar').append(b);});updateHotbar();
}
function updateHotbar(){
 for(const b of document.querySelectorAll('[data-tool]')){const id=b.dataset.tool,tier=TOOL_TIERS[me?.tiers?.[id]||'wood'],bar=b.querySelector('.durability i'),durability=me?.durability?.[id]??(me?0:100);b.classList.toggle('active',id===selected);b.classList.toggle('disabled',!canEquip(me,id));b.setAttribute('aria-disabled',String(!canEquip(me,id)));b.classList.toggle('empty',me&&(foodIds.includes(id)?!me.inventory?.[id]:id==='heal'?false:!durability));if(bar)bar.style.width=Math.min(100,Math.max(0,durability/(tier?.durability||100)*100))+'%';const count=b.querySelector('.slot-count');if(count)count.textContent=foodIds.includes(id)?pretty(me?.inventory?.[id]):'';const badge=b.querySelector('.slot-tier');if(badge)badge.textContent=!canEquip(me,id)?'':me?.tiers?.[id]==='iron'?'III':me?.tiers?.[id]==='stone'?'II':'I';b.title=itemName(id)+(canEquip(me,id)?'':' · Not in your inventory');}
 $('tool-caption').textContent=itemName(selected).toUpperCase()+' · '+(!selected?'Buy your first tool at Oak & Iron':foodIds.includes(selected)?'Eat when you need it':selected==='bow'?'Aim toward an enemy · uses arrows':toolDescriptions[selected]||'');
}
function selectTool(id){if(!canEquip(me,id)){toast(id==='heal'?'Only priests can use a blessing.':'You do not have this item. Visit Oak & Iron for wooden tools.');return;}selected=id;updateHotbar();sendInput();}
renderHotbar();
document.querySelectorAll('[data-auth]').forEach(b=>b.onclick=()=>{mode=b.dataset.auth;auth=null;sessionStorage.removeItem('emberwatch-session');$('password').required=true;$('password').placeholder='At least 8 characters';$('password').autocomplete=mode==='login'?'current-password':'new-password';$('join-button').innerHTML='ENTER THE VILLAGE <span>→</span>';document.querySelectorAll('[data-auth]').forEach(e=>e.classList.toggle('active',e===b));});
if(auth){$('name').value=auth.name;$('password').required=false;$('password').placeholder='Session saved on this tab';$('join-button').innerHTML='RETURN TO THE VILLAGE <span>→</span>';mode='resume';}
async function api(path,options={}){const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',...(auth?{Authorization:'Bearer '+auth.token}:{}),...options.headers}});const data=await response.json();if(!response.ok)throw new Error(data.error||data.message||'The request could not be completed.');return data;}
async function loadVillages(){try{const data=await api('/api/villages');villages=data.villages||[];const before=$('village-select').value;$('village-select').replaceChildren();for(const v of villages){const o=document.createElement('option');o.value=v.id;o.textContent=`${v.name} · Day ${v.day} · ${v.online} online · ${v.residents}/8 residents${v.status==='fallen'?' · Fallen':''}`;$('village-select').append(o);}if(!villages.length){const o=document.createElement('option');o.value='';o.textContent='Found a new village';$('village-select').append(o);requestedVillageName='Emberwatch';}else if(villages.some(v=>v.id===before))$('village-select').value=before;updateVillageDetail();}catch(e){$('form-error').textContent='Cannot reach the game server. '+e.message;}}
function updateVillageDetail(){const v=villages.find(v=>v.id===$('village-select').value);$('village-detail').textContent=v?`${v.online} online · ${Math.max(0,v.residents-v.online)} offline · ${8-v.residents} resident spaces`:'Your watch starts on day one.';}
$('village-select').onchange=()=>{requestedVillageName=null;updateVillageDetail();};$('refresh-villages').onclick=loadVillages;
$('new-village').onclick=()=>{openPanel('<p class="eyebrow">A NEW BEGINNING</p><h2>Found a village</h2><p>Anyone can join an available village. Each village has eight resident places, including residents who are offline.</p><label for="village-name-input">Village name</label><input id="village-name-input" class="create-village-input" maxlength="28" placeholder="The Northern Watch"><button id="confirm-village" class="primary-button">CHOOSE THIS VILLAGE →</button>');$('confirm-village').onclick=()=>{requestedVillageName=$('village-name-input').value.trim()||'The Northern Watch';const option=document.createElement('option');option.value='__new';option.textContent='New village: '+requestedVillageName;$('village-select').append(option);$('village-select').value='__new';updateVillageDetail();dialog.close();};};
$('join-form').onsubmit=async e=>{e.preventDefault();if(connecting)return;connecting=true;$('join-button').disabled=true;$('form-error').textContent='';try{
  if(!auth){auth=await api('/api/auth',{method:'POST',body:JSON.stringify({mode,name:$('name').value.trim(),password:$('password').value})});sessionStorage.setItem('emberwatch-session',JSON.stringify(auth));$('password').value='';$('password').required=false;}
  let v=$('village-select').value;if(requestedVillageName||!v||v==='__new'){const data=await api('/api/villages',{method:'POST',body:JSON.stringify({name:requestedVillageName||'Emberwatch'})});v=data.village?.id||data.id;await loadVillages();}
  villageId=v;connect();
}catch(error){$('form-error').textContent=error.message;connecting=false;$('join-button').disabled=false;if(/session|token|expired|unauthor/i.test(error.message)){auth=null;sessionStorage.removeItem('emberwatch-session');$('password').required=true;}}};
function connect(){intentionalClose=false;clearTimeout(reconnectTimer);socket=new WebSocket(`${location.protocol==='https:'?'wss':'ws'}://${location.host}/socket`);socket.onopen=()=>socket.send(JSON.stringify({type:'join',token:auth.token,villageId,role,statePatches:true}));socket.onmessage=e=>{let m;try{m=JSON.parse(e.data);}catch{return;}if(m.type==='welcome'){
  ownId=m.id||m.playerId;joined=true;connecting=false;chat.setConnected(true);$('join-button').disabled=false;$('lobby').hidden=true;$('hud').hidden=false;$('connection').innerHTML='<i></i> CONNECTED';$('village-name').textContent=villages.find(v=>v.id===villageId)?.name||requestedVillageName||'Emberwatch';requestedVillageName=null;toast('Welcome to the watch. Hold right mouse to look around.');lastStateAt=performance.now();$('world').focus({preventScroll:true});
}else if(m.type==='state'){if(!m.patch)world?.resetResourceEffects?.();state=m.patch?{...state,...m.state}:m.state;if(m.state.plots||!m.patch)dynamicSolids=plotSolids(state.plots||[]);lastStateAt=performance.now();for(const entity of [...state.players,...state.guards,...state.zombies,...(state.workers||[])]){if(entity.id===ownId)continue;let track=poseTracks.get(entity.id);if(!track){track=new PoseBuffer();poseTracks.set(entity.id,track);}track.push(entity,lastStateAt);}const prior=me;me=state.players.find(p=>p.id===ownId);if(me&&!prior){selected=canEquip(me,me.tool)?me.tool:'';predicted.x=me.x;predicted.z=me.z;desired.yaw=me.yaw??Math.PI;cameraYaw=0;role=me.role;if(!savedHotbar&&me.role==='priest'){hotbar[7]='heal';renderHotbar();}}$('connection').innerHTML='<i></i> CONNECTED';if(me){if(prior&&prior.role!==me.role&&!savedHotbar){hotbar[7]=me.role==='priest'?'heal':'good_food';renderHotbar();}if(!canEquip(me,selected))selected='';if(!selected&&canEquip(me,me.tool))selected=me.tool;updateHUD();}}else if(['chat','chatHistory','typing'].includes(m.type)){chat.receive(m);}else if(m.type==='error'||m.type==='notice'){if(!joined&&m.type==='error'){$('form-error').textContent=m.message;connecting=false;$('join-button').disabled=false;intentionalClose=true;socket.close();}else toast(m.message,m.type==='error');}};
  socket.onclose=e=>{chat.setConnected(false);keys.clear();cameraLook?.stop();if(intentionalClose)return;if(joined){$('connection').textContent='RECONNECTING';toast('Connection lost. Rejoining the same village…',true);reconnectTimer=setTimeout(connect,2000);}else{connecting=false;$('join-button').disabled=false;$('form-error').textContent='Could not connect to the village. Refresh and try again.';}};
  socket.onerror=()=>{if(!joined)$('form-error').textContent='The multiplayer connection is unavailable.';};
}
function send(message){if(socket?.readyState===WebSocket.OPEN)socket.send(JSON.stringify(message));}
function inputBlocked(){return dialog.open||chat.isFocused()||['INPUT','SELECT','TEXTAREA'].includes(document.activeElement?.tagName)||document.activeElement?.isContentEditable;}
function gatherNodes(){return [...RESOURCES,...(state?.plotResources||[])];}
function gatherStates(){return [...(state?.resources||[]),...(state?.plotResources||[])];}
function calculateInput(){let r=Number(keys.has('KeyD'))-Number(keys.has('KeyA')),f=Number(keys.has('KeyW'))-Number(keys.has('KeyS'));if(inputBlocked()||me?.downed||me?.carriedBy||me?.bedPlotId||state?.status==='fallen')r=f=0;const length=Math.hypot(r,f)||1;r/=length;f/=length;desired.x=r*Math.cos(cameraYaw)-f*Math.sin(cameraYaw);desired.z=-r*Math.sin(cameraYaw)-f*Math.cos(cameraYaw);if(Math.hypot(desired.x,desired.z)>.1)desired.yaw=Math.atan2(desired.x,desired.z);}
setInterval(()=>{if(joined){calculateInput();sendInput();}},66);
function sendInput(){if(joined)send({type:'input',x:desired.x,z:desired.z,yaw:desired.yaw,sprint:keys.has('ShiftLeft')||keys.has('ShiftRight'),tool:foodIds.includes(selected)?'food':selected});}
function action(kind,extra={}){if(!joined||!me||(me.downed&&kind!=='churchLeave')||dialog.open&&['attack','gather','repair'].includes(kind))return;sendInput();send({type:'action',kind,...extra});}
function useTool(){
 if(!joined||!me||me.downed||inputBlocked()||me.carriedBy||me.bedPlotId)return;
 if(!selected||!canEquip(me,selected)){toast('Choose an owned tool from your hotbar. Buy your first tool at Oak & Iron.');return;}
 const now=performance.now();if(now-lastToolUse<580)return;
 if(foodIds.includes(selected)){lastToolUse=now;action('eat',{tier:selected});return;}
 if(me.mountedHorseId||me.carryingId){toast('Dismount or put down your companion before using equipment.');return;}
 const face=target=>{desired.yaw=Math.atan2(target.x-me.x,target.z-me.z);};
 const swing=()=>{lastToolUse=now;localActionId++;};
 if(selected==='sword'||selected==='bow'){if(selected==='bow')desired.yaw=cameraYaw+Math.PI;swing();action('attack');sound('swing');}
 else if(selected==='hammer'){
   const choices=[{id:'gate',x:0,z:18,...state.gate},{id:'keep',...BUILDINGS.find(b=>b.id==='keep'),...state.keep},...(state.plots||[]).filter(p=>p.building).map(p=>({...PLOTS.find(v=>v.id===p.id),...p,plot:true}))];
   const target=choices.filter(p=>p.hp<p.maxHp).sort((a,b)=>distance(a,me)-distance(b,me))[0];
   if(!target||distance(target,me)>(target.plot?12:target.id==='gate'?9:14)){toast('Move beside a damaged structure to repair it.');return;}
   face(target);swing();action(target.plot?'repairPlot':'repair',target.plot?{plotId:target.id}:{targetId:target.id});sound('tap');
 }else if(selected==='heal'){
   const target=state.players.filter(p=>p.id!==ownId&&p.online&&(p.downed||p.hp<p.maxHp)).sort((a,b)=>distance(a,me)-distance(b,me))[0];
   if(target&&distance(target,me)<=3.5){face(target);swing();action('heal',{targetId:target.id});}else toast('Stand beside a wounded ally to offer a blessing.');
 }else{
   const type={axe:'timber',pickaxe:'stone',scythe:'wheat'}[selected];
   const target=nearestGatherable(me,selected,gatherNodes(),gatherStates());
   if(target&&distance(target,me)<=3.3){face(target);swing();action('gather',{targetId:target.id});sound('tap');}
   else toast('Move closer to '+({timber:'a tree',stone:'a stone outcrop',wheat:'a wheat stalk'}[type])+'.');
 }
}
function distance(a,b){return Math.hypot(a.x-b.x,a.z-b.z);}
function updateInteraction(){
 if(!me||me.downed||inputBlocked()||me.bedPlotId||me.carriedBy){$('interaction').hidden=true;interaction=null;return;}
 const candidate=chooseInteraction(me,selected,gatherNodes(),gatherStates(),BUILDINGS);interaction=null;
 if(candidate?.kind==='gather')interaction={...candidate,title:{timber:'Chop this tree',stone:'Mine this outcrop',iron:'Mine this iron',coal:'Mine this coal',wheat:'Harvest this wheat'}[candidate.resource.type],subtitle:`${TOOL_TIERS[me.tiers?.[selected]||'wood'].yield} resources per swing · Tool durability ${pretty(me.durability?.[selected])}`};
 else if(candidate?.kind==='repair'&&state.gate.hp<state.gate.maxHp)interaction={...candidate,title:'Repair the gate',subtitle:`${pretty(state.gate.hp)} / ${pretty(state.gate.maxHp)} health · Uses village supplies`};
 else if(candidate?.building){const b=candidate.building;interaction={kind:b.kind,id:b.id,title:b.name,subtitle:{bank:'Trade resources, bank savings, and village votes',shop:'Buy basic wooden tools',food:'Buy meals to carry and eat later',church:'Priest care and the church registry',barracks:'Feed and inspect the village watch',stable:'Buy horses and prepare for travel',merchant:'Traveling wares and village exports',keep:'Village policies and council votes'}[b.kind]};}
 if(!interaction){const carried=me.carryingId;const downed=!carried&&state.players.filter(p=>p.online&&p.downed&&!p.carriedBy&&!p.bedPlotId&&p.id!==ownId&&distance(me,p)<=2.5).sort((a,b)=>distance(me,a)-distance(me,b))[0];
  if(downed)interaction={kind:'carry',id:downed.id,title:'Carry '+downed.name,subtitle:'Bring this dwarf to a player church · G to put down'};
  else {const horse=state.horses?.find(h=>h.ownerId===ownId&&distance(me,h)<=3);const cart=state.carts?.find(c=>c.ownerId===ownId&&distance(me,c)<=3);const plot=PLOTS.map(p=>({...p,gap:distance(me,plotFront(p,-.8))})).filter(p=>p.gap<=4.5).sort((a,b)=>a.gap-b.gap)[0];if(horse)interaction={kind:'horse',id:horse.id,title:'Your horse',subtitle:me.mountedHorseId?'Dismount or hitch a cart':'Ride or attach a nearby cart'};else if(cart)interaction={kind:'cart',id:cart.id,title:'Your cargo cart',subtitle:'Store supplies or attach to a horse'};else if(plot){const owned=state.plots?.find(p=>p.id===plot.id);interaction={kind:'plot',id:plot.id,title:plot.name,subtitle:owned?.ownerId===ownId?'Build, stock, and manage your plot':owned?.ownerId?'Visit this player’s plot':'Buy this plot and choose a building'};}}
 }
 $('interaction').hidden=!interaction;if(interaction){$('interaction-title').textContent=interaction.title;$('interaction-subtitle').textContent=interaction.subtitle;}
}
function interact(){updateInteraction();if(!interaction)return;if(['repair','gather'].includes(interaction.kind)){useTool();return;}if(interaction.kind==='carry'){action('carryPlayer',{targetId:interaction.id});return;}const panels={bank:'bank',shop:'tools',food:'food',church:'church',barracks:'barracks',stable:'stable',merchant:'merchant',keep:'policies',plot:'plot',horse:'horse',cart:'cart'};if(panels[interaction.kind])settlement.show(panels[interaction.kind],interaction.id);}
let lastToast='',lastToastAt=0;
function toast(message,error=false){if(!message)return;const now=performance.now();if(message===lastToast&&now-lastToastAt<4500)return;lastToast=message;lastToastAt=now;const e=document.createElement('div');e.className='toast'+(error?' error':'');e.textContent=message;$('toast-area').append(e);while($('toast-area').children.length>2)$('toast-area').firstChild.remove();setTimeout(()=>e.remove(),4500);}
function openPanel(content,panel=null){chat.close();activePanel=panel;dialog.classList.toggle('settlement-dialog',panel==='settlement');keys.clear();desired.x=desired.z=0;sendInput();cameraLook?.stop();$('panel-content').innerHTML=content;if(!dialog.open)dialog.showModal();}
function panelAction(id,kind,extra={},refresh=null){$(id).onclick=()=>{send({type:'action',kind,...extra});if(refresh)setTimeout(refresh,160);};}
function showInventory(){settlement.show('inventory');}
function showBank(){settlement.show('bank');}
function updateBank(){settlement.refresh();}
function showShop(){settlement.show('tools');}
function showFood(){settlement.show('food');}
function showWatch(){settlement.show('barracks');}
function showHelp(){openPanel(`<p class="eyebrow">YOUR FIRST WATCH</p><h2>Know your way around.</h2><div class="help-grid"><kbd>W A S D</kbd><span>Move through the village</span><kbd>Right mouse</kbd><span>Hold and move the mouse to look around, including while downed</span><kbd>Shift</kbd><span>Sprint while your hunger allows</span><kbd>Left click</kbd><span>Use your selected sword, tool, or blessing</span><kbd>1–8 / Scroll</kbd><span>Select a tool or item</span><kbd>E</kbd><span>Interact with a shop, bank, or nearby resource</span><kbd>Enter</kbd><span>Open village chat and type; Enter again to send</span><kbd>T</kbd><span>Toggle the village chat panel</span><kbd>I</kbd><span>Check your pack and village supplies</span><kbd>G</kbd><span>Put down a carried dwarf, leave a bed, or dismount</span><kbd>M</kbd><span>Village atlas and player plots</span><kbd>H</kbd><span>Open these controls</span><kbd>F</kbd><span>Toggle fullscreen</span><kbd>Esc</kbd><span>Release the mouse or open the menu</span></div><h3>Prepare, then stand together.</h3><p>Start with 10 gold and empty hands. Buy one wooden tool at Oak & Iron. Use an axe on trees, a pickaxe on stone, and a scythe on wheat. Sell or donate at the treasury. Watch hunger below your health bar. At night, use your sword to fight or your hammer to repair the gate. A priest’s blessing can save a fallen ally.</p>`);}
function showMenu(){if(!joined)return;openPanel(`<p class="eyebrow">THE WATCH CONTINUES</p><h2>Village menu</h2><p>The village keeps running while another resident is online. When the last resident leaves, time and attacks pause.</p><div class="panel-row"><span>Sound effects</span><button id="sound-toggle">${muted?'Muted':'On'}</button></div><div class="panel-row"><span>Residents</span><strong>${state?.players?.filter(p=>p.online).length||0} online / ${state?.players?.length||0} total</strong></div>${state?.devTools?'<div class="panel-row"><span>Local development control</span><button id="start-night">Start night</button></div>':''}<div class="panel-actions"><button id="menu-help" class="secondary-button">Controls</button><button id="build-status" class="secondary-button">What’s in this build</button><button id="menu-role" class="secondary-button">Change role</button><button id="menu-atlas" class="secondary-button">Village atlas</button><button id="leave-button" class="secondary-button">Leave village</button></div>`);$('sound-toggle').onclick=()=>{muted=!muted;showMenu();};$('menu-help').onclick=showHelp;$('build-status').onclick=showBuildStatus;$('menu-role').onclick=()=>settlement.show('roles');$('menu-atlas').onclick=()=>settlement.show('atlas');$('leave-button').onclick=leave;if($('start-night'))panelAction('start-night','startNight',{},()=>dialog.close());}
function showBuildStatus(){openPanel(`<p class="eyebrow">FIRST LIGHT · BUILD 07</p><h2>A better everyday watch.</h2><p>Walking and running now have more natural joint movement, foot lift, and body motion. Path junctions have cleaner stone borders. New residents start empty-handed with 10 gold. Oak & Iron sells tools and larger backpacks you can see on your dwarf. Hire workers at the treasury, then choose what they gather and where they deliver or sell it from your pack’s Workers menu. New villages start with 20,000 treasury gold. Fallen villages leave the selection list. The treasury accepts your chosen trade quantity. Guards have a recovering shield, priests have more health, and villagers carry more. Stock towers with arrows and barracks with wheat for replacement troops.</p><p>The expanded map has forty village plots, eight exposed outposts, public mines, fields, woodland, and a single shared gate.</p><h3>Build your own part of the village</h3><p>Buy up to five plots, build farms and workshops, stock crafted equipment, recruit defenders, supply towers, and care for fallen allies in church beds. The treasury supports resource trade, protected savings, purchase credit, and village votes reviewed by the steward. The merchant, stables, and cargo carts support longer journeys.</p><h3>Keep testing with us</h3><p>Costs and combat balance are initial settings. Tell us if a building is difficult to reach, a troop gets stuck, or a purchase behaves differently from its displayed cost. Animation, accessibility, and performance improvements continue.</p>`);}
function leave(){world?.resetResourceEffects?.();selected='';cameraLook.stop();settlement.clear();chat.reset();chat.setConnected(false);intentionalClose=true;clearTimeout(reconnectTimer);socket?.close();joined=false;connecting=false;state=null;me=null;ownId=null;keys.clear();dialog.close();$('lobby').hidden=false;$('hud').hidden=true;$('join-button').disabled=false;for(const a of actors.values()){scene.remove(a.rig.group);a.rig.dispose();a.label?.remove();}actors.clear();poseTracks.clear();localActionId=0;lastToolUse=-Infinity;loadVillages();}
document.querySelector('.close-dialog').onclick=()=>dialog.close();dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});
$('help-button').onclick=showHelp;$('menu-button').onclick=showMenu;$('inventory-button').onclick=showInventory;$('interaction').onclick=interact;$('new-run-button').onclick=leave;
$('map-button').onclick=()=>settlement.show('atlas');
$('respawn-button').onclick=()=>send({type:'action',kind:'respawn'});
$('tour-button').onclick=()=>{tour=!tour;$('welcome')?.classList.toggle('tour',tour);$('tour-button').textContent=tour?'Return to the village view ↙':'Explore the view ↗';};
function updateHUD(){const night=state.phase==='night';$('day-label').textContent=(night?'NIGHT ':'DAY ')+state.day;$('phase-icon').textContent=night?'☾':'☀';$('phase-label').textContent=night?'Hold until dawn':'Time to prepare';const seconds=Math.max(0,Math.ceil(state.phaseRemaining));$('clock').textContent=String(Math.floor(seconds/60)).padStart(2,'0')+':'+String(seconds%60).padStart(2,'0');
 $('objective-title').textContent=night?'Hold the gate together':'Make ready for nightfall';$('objective-text').textContent=night?'Keep the Hearthkeep standing. Repair the gate and protect your allies.':'Gather supplies outside the walls. Stand with the watch when darkness falls.';$('threat-label').textContent=night?`${state.zombies.length} zombies on the road`:(state.day%5===0?'A stronger horde approaches next night':'The graveyard stirs at dusk');
 for(const type of ['gate','keep']){const s=state[type];$(`${type}-text`).textContent=pretty(s.hp)+' / '+pretty(s.maxHp);$(`${type}-bar`).style.width=Math.max(0,s.hp/s.maxHp*100)+'%';}
 $('player-name').textContent=me.name;$('player-role').textContent=me.role.toUpperCase();$('role-portrait').innerHTML=icon(me.role);$('wallet').textContent=pretty(me.wallet);$('hp-text').textContent=pretty(me.hp)+' / '+pretty(me.maxHp);$('hp-bar').style.width=Math.max(0,me.hp/me.maxHp*100)+'%';$('shield-status').hidden=!(me.maxShield>0);$('shield-text').textContent=pretty(me.shield)+' / '+pretty(me.maxShield);$('shield-bar').style.width=(me.maxShield?Math.max(0,me.shield/me.maxShield*100):0)+'%';$('supplies').innerHTML=['timber','stone','wheat'].map(r=>`<span title="${r}">${icon(r)}${pretty(me.inventory?.[r])}</span>`).join('');
 const hunger=Math.max(0,Math.min(100,Math.floor(me.hunger??100)));$('hunger-text').textContent=hunger+' / 100';$('hunger-bar').style.width=hunger+'%';$('hunger-meter').setAttribute('aria-valuenow',hunger);$('hunger-meter').classList.toggle('low',hunger<=25);$('hunger-label').textContent=hunger===0?'HUNGER · EAT TO SPRINT':hunger<=25?'HUNGER · LOW':'HUNGER';updateBank();
 const online=state.players.filter(p=>p.online).length;$('residents-label').textContent=online+' ONLINE · '+(state.players.length-online)+' OFFLINE';$('location-label').textContent=me.z>23?'BEYOND THE GATE':Math.abs(me.x)>36?'THE HEARTH DISTRICTS':me.z<-55?'NORTHERN VILLAGE':'VILLAGE SQUARE';
 $('carry-status').textContent=`PACK ${inventoryWeight(me)} / ${carryCapacity(me)}`+(me.carryingId?' · CARRYING A DWARF · G TO PUT DOWN':me.mountedHorseId?' · MOUNTED · G TO DISMOUNT':me.bedPlotId?' · RECOVERING IN BED · G TO LEAVE':me.carriedBy?' · AN ALLY IS CARRYING YOU':'');
 updateHotbar();
 $('downed').hidden=!me.downed||state.status==='fallen';$('fallen').hidden=state.status!=='fallen';$('respawn-button').disabled=!me.respawnAvailable;$('respawn-button').textContent=me.respawnAvailable?'RESPAWN · LOSE INVENTORY & 25% WALLET':'WAITING FOR DAWN';$('downed-message').textContent=me.respawnAvailable?'Dawn has arrived. You may respawn now, or keep waiting for a priest to save your belongings.':'A priest can bring you back. You can choose to respawn when dawn arrives.';
}
window.addEventListener('keydown',e=>{if(dialog.open||chat.isFocused()||['INPUT','SELECT','TEXTAREA'].includes(e.target.tagName)||e.target.isContentEditable)return;if(!joined)return;if(e.code==='Enter'||e.code==='NumpadEnter'){e.preventDefault();if(!e.repeat)chat.focus();return;}if(e.code==='KeyT'){e.preventDefault();if(!e.repeat)chat.toggle();return;}if(['Space','Tab'].includes(e.code))e.preventDefault();keys.add(e.code);calculateInput();sendInput();if(e.repeat)return;if(/^Digit[1-8]$/.test(e.code))selectTool(hotbar[Number(e.code.at(-1))-1]);if(e.code==='KeyI')showInventory();if(e.code==='KeyM')settlement.show('atlas');if(e.code==='KeyG'){if(me?.carryingId)action('dropPlayer');else if(me?.mountedHorseId)action('dismountHorse');else if(me?.bedPlotId)action('churchLeave');}if(e.code==='KeyE')interact();if(e.code==='KeyH')showHelp();if(e.code==='Escape')showMenu();if(e.code==='KeyF'){if(document.fullscreenElement)document.exitFullscreen();else document.documentElement.requestFullscreen().catch(()=>{});}});
window.addEventListener('keyup',e=>{keys.delete(e.code);calculateInput();sendInput();});window.addEventListener('blur',()=>{keys.clear();cameraLook?.stop();desired.x=desired.z=0;sendInput();});
cameraLook=bindCameraLook({surfaces:[$('world'),$('downed'),$('fallen')],host:window,enabled:()=>joined&&!inputBlocked(),rotate:(x,y)=>{cameraYaw-=x*.005;cameraPitch=THREE.MathUtils.clamp(cameraPitch+y*.003,.12,.95);}});
$('world').addEventListener('mousedown',e=>{if(e.button===0&&joined&&!inputBlocked())useTool();});
$('world').addEventListener('wheel',e=>{if(!joined||inputBlocked())return;e.preventDefault();const usable=hotbar.filter(id=>canEquip(me,id));if(!usable.length)return;let i=usable.indexOf(selected);selectTool(usable[(i+(e.deltaY>0?1:-1)+usable.length)%usable.length]);},{passive:false});
function sound(kind){if(muted)return;try{audioContext ||=new (window.AudioContext||window.webkitAudioContext)();if(audioContext.state==='suspended')audioContext.resume();const length=.11,buffer=audioContext.createBuffer(1,audioContext.sampleRate*length,audioContext.sampleRate),samples=buffer.getChannelData(0);for(let i=0;i<samples.length;i++)samples[i]=(Math.random()*2-1)*Math.pow(1-i/samples.length,2);const source=audioContext.createBufferSource(),filter=audioContext.createBiquadFilter(),gain=audioContext.createGain();source.buffer=buffer;filter.type='lowpass';filter.frequency.value=kind==='tap'?650:1600;gain.gain.value=kind==='tap'?.1:.04;source.connect(filter);filter.connect(gain);gain.connect(audioContext.destination);source.start();}catch{}}

let scene,renderer,camera,world,sun,skyLight,nightMix=0;
function fail(error){$('loading').hidden=true;$('fatal').hidden=false;$('fatal-message').textContent='A WebGL-capable desktop browser is required. '+(error.message||error);console.error(error);}
async function boot(){
 scene=new THREE.Scene();scene.background=new THREE.Color('#b5c9bd');scene.fog=new THREE.FogExp2('#b5c9bd',.006);
 renderer=new THREE.WebGLRenderer({canvas:$('world'),antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.6));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.14;
 camera=new THREE.PerspectiveCamera(52,innerWidth/innerHeight,.1,360);camera.position.set(33,24,50);camera.lookAt(0,3,-8);
 skyLight=new THREE.HemisphereLight('#e6eddb','#354542',2.5);scene.add(skyLight);sun=new THREE.DirectionalLight('#ffe3ad',3.3);sun.position.set(-75,140,90);sun.target.position.set(0,0,-30);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);sun.shadow.camera.left=-140;sun.shadow.camera.right=140;sun.shadow.camera.top=155;sun.shadow.camera.bottom=-155;sun.shadow.camera.near=1;sun.shadow.camera.far=400;sun.shadow.bias=-.0005;sun.shadow.normalBias=.035;scene.add(sun,sun.target);
 const fill=new THREE.DirectionalLight('#bddce1',.5);fill.position.set(40,25,-45);scene.add(fill);world=createWorld(scene);const transportWorld=createTransportWorld(scene);
 const menuActors=[];for(const [kind,x,z,yaw] of [['guard',-2,24,0],['guard',2,28,.15],['villager',-4,6,-.5],['priest',8,-7,-1]]){const rig=createCharacter(kind,menuActors.length+1);rig.group.position.set(x,0,z);rig.group.rotation.y=yaw;scene.add(rig.group);menuActors.push(rig);}
 const dustGeo=new THREE.BufferGeometry(),dustPositions=new Float32Array(80*3);for(let i=0;i<80;i++){dustPositions[i*3]=(Math.random()-.5)*70;dustPositions[i*3+1]=Math.random()*7+.8;dustPositions[i*3+2]=Math.random()*80-40;}dustGeo.setAttribute('position',new THREE.BufferAttribute(dustPositions,3));const dustMat=new THREE.PointsMaterial({color:'#ffe7ac',size:.075,transparent:true,opacity:.45,depthWrite:false});const dust=new THREE.Points(dustGeo,dustMat);scene.add(dust);
 const clock=new THREE.Clock(),camTarget=new THREE.Vector3(),camDesired=new THREE.Vector3();let elapsed=0,netElapsed=0,interactionElapsed=0;
 function frame(){const rawDt=clock.getDelta(),dt=Math.min(rawDt,.05);elapsed+=rawDt;requestAnimationFrame(frame);menuActors.forEach((r,i)=>{r.group.visible=!joined;r.update(dt,elapsed,{moving:false,tool:i<2?'sword':i===3?'heal':'axe'});});
   const targetNight=joined&&state?.phase==='night'?1:0;nightMix=THREE.MathUtils.lerp(nightMix,targetNight,1-Math.exp(-rawDt*.8));scene.background.set('#b5c9bd').lerp(new THREE.Color('#182c43'),nightMix);scene.fog.color.copy(scene.background);scene.fog.density=.006+nightMix*.002;skyLight.intensity=2.5-nightMix*1.65;sun.intensity=3.3-nightMix*2.6;sun.color.set('#ffe3ad').lerp(new THREE.Color('#b5c8ff'),nightMix);dustMat.opacity=.22+nightMix*.4;dust.rotation.y=elapsed*.006;
   world.update(elapsed,nightMix,state||{});
   if(joined&&me){
     let right=Number(keys.has('KeyD'))-Number(keys.has('KeyA')),forward=Number(keys.has('KeyW'))-Number(keys.has('KeyS'));if(inputBlocked()||me.downed||me.carriedBy||me.bedPlotId||state.status==='fallen')right=forward=0;const len=Math.hypot(right,forward)||1;right/=len;forward/=len;desired.x=right*Math.cos(cameraYaw)-forward*Math.sin(cameraYaw);desired.z=-right*Math.sin(cameraYaw)-forward*Math.cos(cameraYaw);if(Math.hypot(desired.x,desired.z)>.1)desired.yaw=Math.atan2(desired.x,desired.z);
     const sprint=(keys.has('ShiftLeft')||keys.has('ShiftRight'))&&(me.hunger??100)>0;let speed=me.mountedHorseId?TRANSPORT.horseSpeed:sprint?CONFIG.sprintSpeed:CONFIG.speed;if(me.carryingId)speed=CONFIG.speed*.55;if(inventoryWeight(me)>carryCapacity(me))speed*=.65;if(Math.hypot(predicted.x-me.x,predicted.z-me.z)>3){predicted.x=me.x;predicted.z=me.z;}else {predicted.x=THREE.MathUtils.lerp(predicted.x,me.x,1-Math.exp(-dt*4));predicted.z=THREE.MathUtils.lerp(predicted.z,me.z,1-Math.exp(-dt*4));}if(performance.now()-lastStateAt<1500)moveWithCollision(predicted,desired.x*speed*dt,desired.z*speed*dt,me.mountedHorseId?.8:CONFIG.playerRadius,dynamicSolids);
     interactionElapsed+=dt;if(interactionElapsed>.15){interactionElapsed=0;updateInteraction();drawMap();}
     const activeIds=new Set();const entities=[...state.players.filter(p=>p.online),...state.guards.map(g=>({...g,role:'guard'})),...state.zombies.map(z=>({...z,role:'zombie'})),...(state.workers||[])];
     for(const entity of entities){activeIds.add(entity.id);let a=actors.get(entity.id);if(!a){const rig=createCharacter(entity.role||'villager',hash(entity.id));scene.add(rig.group);rig.group.position.set(entity.x,0,entity.z);const label=document.createElement('div');label.className='nameplate'+(entity.role==='zombie'?' enemy':'');label.innerHTML='<span></span><i><b></b></i><div class="speech-bubble" hidden></div>';$('nameplates').append(label);a={rig,label,role:entity.role,lastX:entity.x,lastZ:entity.z};actors.set(entity.id,a);}if(a.role!==entity.role){a.rig.setRole(entity.role);a.role=entity.role;}
       const group=a.rig.group,isMe=entity.id===ownId,pose=isMe?null:poseTracks.get(entity.id)?.sample(performance.now());
       const x=isMe?predicted.x:(pose?.x??entity.x),z=isMe?predicted.z:(pose?.z??entity.z);
       if(isMe){group.position.x=THREE.MathUtils.lerp(group.position.x,x,1-Math.exp(-dt*28));group.position.z=THREE.MathUtils.lerp(group.position.z,z,1-Math.exp(-dt*28));}
       else{group.position.x=x;group.position.z=z;}
       group.position.y=entity.mountedHorseId?1.35:entity.carriedBy?1.25:entity.bedPlotId ? .45 : 0;
       const yaw=isMe?desired.yaw:(pose?.yaw??entity.yaw??0);let d=yaw-group.rotation.y;d=Math.atan2(Math.sin(d),Math.cos(d));group.rotation.y+=d*(1-Math.exp(-dt*14));
       const renderedSpeed=Math.hypot(group.position.x-a.lastX,group.position.z-a.lastZ)/Math.max(rawDt,.001);a.lastX=group.position.x;a.lastZ=group.position.z;
       const motionSpeed=isMe?Math.min(speed,renderedSpeed):(pose?.speed??0),moving=motionSpeed>.12&&!entity.downed&&entity.hp>0&&!entity.mountedHorseId&&!entity.carriedBy&&!entity.bedPlotId;
       const remoteAnim=pose?.anim??entity.anim;
       a.rig.update(dt,elapsed,{moving,speed:motionSpeed,attack:isMe?localActionId:['attack','attacking','gather','repair'].includes(remoteAnim),channeling:isMe?(me.healRemaining??0)>0:remoteAnim==='heal',downed:entity.downed||entity.hp<=0||Boolean(entity.bedPlotId),mounted:!!entity.mountedHorseId,carrying:!!entity.carryingId,carriedBy:entity.carriedBy,turnRate:d*(1-Math.exp(-dt*14))/Math.max(dt,.001),backpackTier:entity.backpackTier??0,tier:{wood:1,stone:2,iron:3}[entity.tiers?.[isMe?selected:entity.tool]]||1,tool:isMe?(foodIds.includes(selected)?'food':selected):entity.tool??(entity.role==='zombie'?'':'sword')});

     }for(const [id,a] of actors)if(!activeIds.has(id)){scene.remove(a.rig.group);a.rig.dispose();a.label?.remove();actors.delete(id);poseTracks.delete(id);}
     transportWorld.update(state,dt,new Map(state.players.filter(p=>p.online&&p.mountedHorseId).map(p=>{const g=actors.get(p.id)?.rig.group;return [p.id,g?{x:g.position.x,z:g.position.z,yaw:g.rotation.y}:{x:p.x,z:p.z,yaw:p.yaw}];})));
     camTarget.set(predicted.x,1.45,predicted.z);camDesired.set(predicted.x+Math.sin(cameraYaw)*cameraDistance*Math.cos(cameraPitch),1.45+Math.sin(cameraPitch)*cameraDistance,predicted.z+Math.cos(cameraYaw)*cameraDistance*Math.cos(cameraPitch));
     // Keep the camera in front of solid building/wall bounds without mesh raycast overhead.
     for(let t=1;t>0.18;t-=.08){const p=camTarget.clone().lerp(camDesired,t);if(cameraBlocked(p)){camDesired.copy(camTarget.clone().lerp(camDesired,Math.max(.18,t-.1)));}}
     camera.position.lerp(camDesired,1-Math.exp(-dt*10));camera.lookAt(camTarget);camera.updateMatrixWorld();for(const entity of entities){const a=actors.get(entity.id);if(!a)continue;const v=a.rig.group.position.clone();v.y=2.8;const far=distance(entity,predicted)>20;v.project(camera);const bubble=chat.bubbleFor(entity.id),bubbleElement=a.label.querySelector('.speech-bubble');bubbleElement.hidden=!bubble;if(bubble){bubbleElement.textContent=bubble.typing?'…':bubble.text;bubbleElement.classList.toggle('typing',bubble.typing);}a.label.classList.toggle('self',entity.id===ownId);a.label.hidden=(entity.id===ownId&&!bubble)||far||v.z>1||v.z<0||Math.abs(v.x)>1||Math.abs(v.y)>1;if(!a.label.hidden){a.label.style.transform=`translate(${(v.x*.5+.5)*innerWidth}px,${(-v.y*.5+.5)*innerHeight}px) translate(-50%,-100%)`;a.label.querySelector('span').textContent=entity.name||(entity.role==='zombie'?'Restless dead':'Village watch');a.label.querySelector('b').style.width=Math.max(0,entity.hp/entity.maxHp*100)+'%';}}if(performance.now()-lastStateAt>2000)$('connection').textContent='WAITING FOR SERVER';
   }else{transportWorld.update(state||{},dt);const angle=Math.sin(elapsed*.025)*(tour?.35:.08),radius=tour?63:55;camDesired.set(Math.sin(.63+angle)*radius,tour?30:24,Math.cos(.63+angle)*radius);camera.position.lerp(camDesired,.012);camera.lookAt(0,3,-8);}
   renderer.render(scene,camera);
 }
 function cameraBlocked(p){return [...BUILDINGS,...dynamicSolids].some(b=>Math.abs(p.x-b.x)<b.w/2+.5&&Math.abs(p.z-b.z)<b.d/2+.5&&p.y<9)||WALLS.some(b=>Math.abs(p.x-b.x)<b.w/2+.5&&Math.abs(p.z-b.z)<b.d/2+.5&&p.y<5.3);}
 window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);});
 $('loading').hidden=true;await loadVillages();frame();
 // Read-only inspection hook for local visual/network QA; exposes no account tokens or mutations.
 window.__emberwatch={get state(){return state;},get me(){return me;},get rendererInfo(){return renderer.info.render;},get controls(){return {pressed:[...keys],desired:{...desired},joined};}};
}
function hash(s){let h=0;for(const c of String(s))h=(Math.imul(h,31)+c.charCodeAt(0))|0;return Math.abs(h);}
function drawMap(){
 if(!me)return;const c=$('minimap').getContext('2d'),w=220,pad=9,scale=Math.min((w-pad*2)/(WORLD_BOUNDS.maxX-WORLD_BOUNDS.minX),(w-pad*2)/(WORLD_BOUNDS.maxZ-WORLD_BOUNDS.minZ));const x0=(w-(WORLD_BOUNDS.maxX-WORLD_BOUNDS.minX)*scale)/2,z0=(w-(WORLD_BOUNDS.maxZ-WORLD_BOUNDS.minZ)*scale)/2;const transform=(x,z)=>[x0+(x-WORLD_BOUNDS.minX)*scale,z0+(z-WORLD_BOUNDS.minZ)*scale];
 c.clearRect(0,0,w,w);c.fillStyle='#213832';c.fillRect(0,0,w,w);c.fillStyle='#334936';const corner=transform(-87,-132);c.fillRect(corner[0],corner[1],174*scale,150*scale);
 c.strokeStyle='#9b9980';c.lineWidth=1.5;for(const wall of WALLS){const[x,y]=transform(wall.x-wall.w/2,wall.z-wall.d/2);c.fillStyle='#9b9980';c.fillRect(x,y,Math.max(1,wall.w*scale),Math.max(1,wall.d*scale));}
 c.strokeStyle='#89947b';c.lineWidth=3;c.beginPath();ROAD.forEach((p,i)=>{const[x,y]=transform(p.x,p.z);i?c.lineTo(x,y):c.moveTo(x,y);});c.stroke();
 for(const place of PLOTS){const p=state.plots?.find(p=>p.id===place.id),[x,y]=transform(place.x-place.w/2,place.z-place.d/2);c.fillStyle=p?.ownerId===ownId?'#d3b675':p?.building?'#8aa28a':place.outside?'#866b4f':'#526e57';c.fillRect(x,y,place.w*scale,place.d*scale);c.strokeStyle='#aac28a55';c.lineWidth=.6;c.strokeRect(x,y,place.w*scale,place.d*scale);}
 for(const b of BUILDINGS){const[x,y]=transform(b.x-b.w/2,b.z-b.d/2);c.fillStyle=b.kind==='keep'?'#d5c597':'#a9b69a';c.fillRect(x,y,b.w*scale,b.d*scale);}
 const point=(entity,color,radius)=>{const[x,y]=transform(entity.x,entity.z);c.fillStyle=color;c.beginPath();c.arc(x,y,radius,0,Math.PI*2);c.fill();};
 for(const p of [...state.guards,...state.players.filter(p=>p.online)])if(p.id!==ownId&&p.hp>0)point(p,'#9dd4d7',1.8);for(const p of state.zombies)point(p,'#e7896a',1.7);for(const worker of state.workers||[])point(worker,worker.ownerId===ownId?'#e7be76':'#c1ad8e',1.8);
 let waypoint=settlement.getWaypoint();if(waypoint?.kind==='worker')waypoint=state.workers?.find(w=>w.id===waypoint.id)?{...state.workers.find(w=>w.id===waypoint.id)}:null;if(waypoint?.kind==='horse')waypoint=state.horses?.find(h=>h.id===waypoint.id)?{...state.horses.find(h=>h.id===waypoint.id),name:'Your horse'}:null;if(waypoint&&Number.isFinite(waypoint.x)){const[x,y]=transform(waypoint.x,waypoint.z);c.strokeStyle='#f5d38b';c.lineWidth=1.5;c.beginPath();c.arc(x,y,5,0,Math.PI*2);c.stroke();$('waypoint-label').textContent=waypoint.name+' · '+Math.round(distance(me,waypoint))+' m';}else $('waypoint-label').textContent='M · ATLAS & PLAYER SHOPS';
 const[x,y]=transform(predicted.x,predicted.z);c.save();c.translate(x,y);c.rotate(-desired.yaw+Math.PI);c.fillStyle='#ffe2a3';c.shadowColor='#e4be77';c.shadowBlur=6;c.beginPath();c.moveTo(0,-4);c.lineTo(3,3);c.lineTo(0,1.5);c.lineTo(-3,3);c.closePath();c.fill();c.restore();
}
boot().catch(fail);
